-- ============================================================
-- CAT HUB ANTI GUARD
-- Made by Chris
-- ============================================================
local proximityPromptService = game:GetService("ProximityPromptService")
local runService = game:GetService("RunService")
local tweenService = game:GetService("TweenService")
local userInputService = game:GetService("UserInputService")
local players = game:GetService("Players")

-- 🔵 BLUE THEME
local color = Color3.fromRGB(35, 125, 255)
local color2 = Color3.fromRGB(100, 180, 255)
local color3 = Color3.fromRGB(255, 255, 255)
local color4 = Color3.fromRGB(8, 12, 22)
local color5 = Color3.fromRGB(0, 220, 100)
local color6 = Color3.fromRGB(255, 60, 60)

local localPlayer = players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- Waypoints
local v1 = {
    Vector3.new(500.62, 70.28, -366.64), Vector3.new(508.3, 70.28, -366.03),
    Vector3.new(519.43, 70.28, -366.47), Vector3.new(529.22, 70.28, -366.71),
    Vector3.new(546.8, 70.28, -364.4),
}

-- ✅ ANTI-GUARD ON BY DEFAULT
local v2 = true
local isOnCooldown = false

-- Destroy old GUI
local oldGui = playerGui:FindFirstChild("CAT_HUB_ANTI_GUARD")
if oldGui then oldGui:Destroy() end

-- ============================================================
-- SCREENGUI
-- ============================================================
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CAT_HUB_ANTI_GUARD"
screenGui.ResetOnSpawn = false
screenGui.IgnoreGuiInset = true
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- ============================================================
-- FLOATING CIRCLE BUTTON (DRAGGABLE)
-- ============================================================
local floatBtn = Instance.new("ImageButton")
floatBtn.Name = "CAT_FLOAT"
floatBtn.Size = UDim2.fromOffset(54, 54)
floatBtn.Position = UDim2.new(1, -68, 0, 16)
floatBtn.BackgroundColor3 = color4
floatBtn.BackgroundTransparency = 0.3
floatBtn.BorderSizePixel = 0
floatBtn.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=85422381974829&width=420&height=420&format=png"
floatBtn.ImageColor3 = color3
floatBtn.Active = true
floatBtn.ZIndex = 200
floatBtn.Parent = screenGui

local uiCorner = Instance.new("UICorner")
uiCorner.CornerRadius = UDim.new(1, 0)
uiCorner.Parent = floatBtn

local uiStroke = Instance.new("UIStroke")
uiStroke.Thickness = 2
uiStroke.Color = color
uiStroke.Parent = floatBtn

task.spawn(function()
    while floatBtn.Parent do
        tweenService:Create(uiStroke, TweenInfo.new(0.45), { Transparency = 0.6, Color = color2 }):Play()
        task.wait(0.45)
        tweenService:Create(uiStroke, TweenInfo.new(0.9), { Transparency = 0, Color = color }):Play()
        task.wait(0.9)
    end
end)

-- ============================================================
-- CIRCLE DRAG (tap = show panel, drag = move circle)
-- ============================================================
local circleDrag = false
local circleMoved = false
local circleStart, circleStartPos

floatBtn.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch
    or input.UserInputType == Enum.UserInputType.MouseButton1 then
        circleDrag = true
        circleMoved = false
        circleStart = input.Position
        circleStartPos = floatBtn.Position
    end
end)

userInputService.InputChanged:Connect(function(input)
    if circleDrag and (input.UserInputType == Enum.UserInputType.Touch
    or input.UserInputType == Enum.UserInputType.MouseMovement) then
        local delta = input.Position - circleStart
        if math.abs(delta.X) > 5 or math.abs(delta.Y) > 5 then
            circleMoved = true
        end
        floatBtn.Position = UDim2.new(
            circleStartPos.X.Scale, circleStartPos.X.Offset + delta.X,
            circleStartPos.Y.Scale, circleStartPos.Y.Offset + delta.Y
        )
    end
end)

userInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Touch
    or input.UserInputType == Enum.UserInputType.MouseButton1 then
        if circleDrag and not circleMoved then
            -- Tap lang → show/hide panel
            if panel then
                panel.Visible = not panel.Visible
            end
        end
        circleDrag = false
        circleMoved = false
    end
end)

-- ============================================================
-- MAIN PANEL (HIDDEN INITIALLY)
-- ============================================================
local panel = Instance.new("Frame")
panel.Name = "CAT_PANEL"
panel.Size = UDim2.fromOffset(265, 190)
panel.Position = UDim2.new(1, -275, 0, 76)
panel.BackgroundColor3 = color4
panel.BackgroundTransparency = 0.15
panel.BorderSizePixel = 0
panel.ClipsDescendants = true
panel.ZIndex = 14
panel.Visible = false  -- ✅ HIDDEN MUNA
panel.Parent = screenGui

local uiCorner2 = Instance.new("UICorner")
uiCorner2.CornerRadius = UDim.new(0, 18)
uiCorner2.Parent = panel

local uiStroke2 = Instance.new("UIStroke")
uiStroke2.Thickness = 2
uiStroke2.Color = color
uiStroke2.Parent = panel

task.spawn(function()
    while panel.Parent do
        tweenService:Create(uiStroke2, TweenInfo.new(0.43), { Transparency = 0.6, Color = color2 }):Play()
        task.wait(0.43)
        tweenService:Create(uiStroke2, TweenInfo.new(1), { Transparency = 0, Color = color }):Play()
        task.wait(1)
    end
end)

-- Top bar
local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1, 0, 0, 4)
topBar.BackgroundColor3 = color2
topBar.BorderSizePixel = 0
topBar.ZIndex = 20
topBar.Parent = panel

local uiCorner3 = Instance.new("UICorner")
uiCorner3.CornerRadius = UDim.new(0, 18)
uiCorner3.Parent = topBar

local uiGradient = Instance.new("UIGradient")
uiGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 40, 120)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(100, 180, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 40, 120)),
})
uiGradient.Parent = topBar

task.spawn(function()
    while topBar.Parent do
        tweenService:Create(uiGradient, TweenInfo.new(
            2.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true
        ), { Offset = Vector2.new(0.6, 0) }):Play()
        task.wait(2.5)
    end
end)

-- Particles
for i = 1, 15 do
    local particle = Instance.new("Frame")
    local size = math.random(2, 4)
    particle.Size = UDim2.fromOffset(size, size)
    particle.Position = UDim2.new(math.random(5, 95) / 100, 0, math.random(5, 95) / 100, 0)
    particle.BackgroundColor3 = color2
    particle.BackgroundTransparency = math.random(30, 75) / 100
    particle.BorderSizePixel = 0
    particle.ZIndex = 16
    particle.Parent = panel
    
    local uiCorner4 = Instance.new("UICorner")
    uiCorner4.CornerRadius = UDim.new(1, 0)
    uiCorner4.Parent = particle
end

-- Title
local titleLabel = Instance.new("TextLabel")
titleLabel.BackgroundTransparency = 1
titleLabel.Position = UDim2.fromOffset(14, 10)
titleLabel.Size = UDim2.new(1, -28, 0, 26)
titleLabel.Text = "🐱 CAT HUB"
titleLabel.TextColor3 = color2
titleLabel.Font = Enum.Font.GothamBlack
titleLabel.TextSize = 17
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.ZIndex = 50
titleLabel.Parent = panel

task.spawn(function()
    local tweenInfo = TweenInfo.new(0.9, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut)
    while titleLabel.Parent do
        tweenService:Create(titleLabel, tweenInfo, { TextColor3 = color3 }):Play()
        task.wait(0.9)
        tweenService:Create(titleLabel, tweenInfo, { TextColor3 = color2 }):Play()
        task.wait(0.9)
    end
end)

-- Subtitle
local subtitleLabel = Instance.new("TextLabel")
subtitleLabel.BackgroundTransparency = 1
subtitleLabel.Position = UDim2.fromOffset(14, 34)
subtitleLabel.Size = UDim2.new(1, -28, 0, 16)
subtitleLabel.Text = "[ ANTI GUARD ]"
subtitleLabel.TextColor3 = Color3.fromRGB(100, 160, 255)
subtitleLabel.Font = Enum.Font.Code
subtitleLabel.TextSize = 8
subtitleLabel.TextXAlignment = Enum.TextXAlignment.Left
subtitleLabel.ZIndex = 50
subtitleLabel.Parent = panel

-- Made by Chris
local madeByLabel = Instance.new("TextLabel")
madeByLabel.BackgroundTransparency = 1
madeByLabel.Position = UDim2.fromOffset(14, 50)
madeByLabel.Size = UDim2.new(1, -28, 0, 12)
madeByLabel.Text = "Made by Chris"
madeByLabel.TextColor3 = Color3.fromRGB(150, 180, 220)
madeByLabel.Font = Enum.Font.Gotham
madeByLabel.TextSize = 8
madeByLabel.TextXAlignment = Enum.TextXAlignment.Left
madeByLabel.ZIndex = 50
madeByLabel.Parent = panel

-- Divider
local divider = Instance.new("Frame")
divider.Position = UDim2.fromOffset(10, 68)
divider.Size = UDim2.new(1, -20, 0, 1)
divider.BackgroundColor3 = color
divider.BackgroundTransparency = 0.3
divider.BorderSizePixel = 0
divider.ZIndex = 50
divider.Parent = panel

-- Status Label
local statusLabel = Instance.new("TextLabel")
statusLabel.Position = UDim2.fromOffset(10, 78)
statusLabel.Size = UDim2.new(1, -20, 0, 24)
statusLabel.BackgroundColor3 = Color3.fromRGB(15, 25, 45)
statusLabel.BorderSizePixel = 0
statusLabel.Text = "   ●  SYSTEM READY"
statusLabel.TextColor3 = color5
statusLabel.Font = Enum.Font.GothamBold
statusLabel.TextSize = 11
statusLabel.TextXAlignment = Enum.TextXAlignment.Left
statusLabel.ZIndex = 50
statusLabel.Parent = panel

local uiCorner5 = Instance.new("UICorner")
uiCorner5.CornerRadius = UDim.new(0, 8)
uiCorner5.Parent = statusLabel

-- Anti Guard Button
local antiGuardBtn = Instance.new("TextButton")
antiGuardBtn.Position = UDim2.fromOffset(10, 112)
antiGuardBtn.Size = UDim2.new(1, -20, 0, 45)
antiGuardBtn.BackgroundColor3 = Color3.fromRGB(15, 25, 45)
antiGuardBtn.BorderSizePixel = 0
antiGuardBtn.RichText = true
antiGuardBtn.Text = '<font color="rgb(255,255,255)">[+] ANTI GUARD • </font><font color="rgb(0,220,100)">ON</font>'
antiGuardBtn.TextColor3 = color3
antiGuardBtn.Font = Enum.Font.GothamBlack
antiGuardBtn.TextSize = 13
antiGuardBtn.AutoButtonColor = false
antiGuardBtn.ZIndex = 60
antiGuardBtn.Parent = panel

local uiCorner6 = Instance.new("UICorner")
uiCorner6.CornerRadius = UDim.new(0, 12)
uiCorner6.Parent = antiGuardBtn

local uiStroke3 = Instance.new("UIStroke")
uiStroke3.Thickness = 2
uiStroke3.Color = color
uiStroke3.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
uiStroke3.Parent = antiGuardBtn

antiGuardBtn.MouseButton1Click:Connect(function()
    v2 = not v2
    if v2 then
        antiGuardBtn.Text = '<font color="rgb(255,255,255)">[+] ANTI GUARD • </font><font color="rgb(0,220,100)">ON</font>'
        statusLabel.Text = "   ●  SYSTEM READY"
        statusLabel.TextColor3 = color5
        uiStroke3.Color = color
    else
        antiGuardBtn.Text = '<font color="rgb(255,255,255)">[-] ANTI GUARD • </font><font color="rgb(255,60,60)">OFF</font>'
        statusLabel.Text = "   ○  SYSTEM DISABLED"
        statusLabel.TextColor3 = color6
        uiStroke3.Color = color6
    end
end)

-- ============================================================
-- DRAG PANEL (via title)
-- ============================================================
local isDragging = false
local dragStart, panelStart

titleLabel.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        isDragging = true
        dragStart = input.Position
        panelStart = panel.Position
    end
end)

userInputService.InputChanged:Connect(function(input)
    if not isDragging then return end
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        local delta = input.Position - dragStart
        panel.Position = UDim2.new(
            panelStart.X.Scale, panelStart.X.Offset + delta.X,
            panelStart.Y.Scale, panelStart.Y.Offset + delta.Y
        )
    end
end)

userInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then
        isDragging = false
    end
end)

-- ============================================================
-- LOADING SCREEN (FULLSCREEN BLACK 5s)
-- ============================================================
local function showLoadingScreen()
    local loadGui = Instance.new("ScreenGui")
    loadGui.Name = "CAT_LOADING"
    loadGui.ResetOnSpawn = false
    loadGui.IgnoreGuiInset = true
    loadGui.DisplayOrder = 999999
    loadGui.Parent = playerGui

    local blackScreen = Instance.new("Frame")
    blackScreen.Size = UDim2.fromScale(1, 1)
    blackScreen.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    blackScreen.BorderSizePixel = 0
    blackScreen.ZIndex = 1
    blackScreen.Parent = loadGui

    local loadImage = Instance.new("ImageLabel")
    loadImage.Size = UDim2.fromOffset(90, 90)
    loadImage.Position = UDim2.new(0.5, -45, 0.5, -70)
    loadImage.BackgroundTransparency = 1
    loadImage.Image = "https://www.roblox.com/asset-thumbnail/image?assetId=85422381974829&width=420&height=420&format=png"
    loadImage.ZIndex = 2
    loadImage.Parent = blackScreen

    local loadText = Instance.new("TextLabel")
    loadText.Size = UDim2.new(1, 0, 0, 30)
    loadText.Position = UDim2.new(0, 0, 0.5, 30)
    loadText.BackgroundTransparency = 1
    loadText.Text = "Loading..."
    loadText.TextColor3 = Color3.fromRGB(255, 255, 255)
    loadText.Font = Enum.Font.GothamBold
    loadText.TextSize = 18
    loadText.ZIndex = 2
    loadText.Parent = blackScreen

    local dots = 0
    local dotLoop = task.spawn(function()
        while loadText.Parent do
            dots = (dots + 1) % 4
            loadText.Text = "Loading" .. string.rep(".", dots)
            task.wait(0.5)
        end
    end)

    task.wait(5)

    task.cancel(dotLoop)
    loadGui:Destroy()
end

-- ============================================================
-- TELEPORT + COOLDOWN
-- ============================================================
local function executeTeleport()
    if not v2 then return end
    if isOnCooldown then return end
    
    isOnCooldown = true
    showLoadingScreen()
    
    local character = localPlayer.Character
    if not character then
        isOnCooldown = false
        return
    end
    
    -- Execute TP
    for index, value in ipairs(v1) do
        if not v2 then
            statusLabel.Text = "   ○  ROUTE STOPPED"
            statusLabel.TextColor3 = color6
            isOnCooldown = false
            return
        end
        statusLabel.Text = "   [TP] POINT " .. index .. "/" .. #v1
        statusLabel.TextColor3 = color3
        character:PivotTo(CFrame.new(value))
        if index < #v1 then
            runService.Heartbeat:Wait()
        end
    end
    
    statusLabel.Text = "   [OK] ROUTE COMPLETE"
    statusLabel.TextColor3 = color5
    
    -- Cooldown 10s
    local cooldown = 10
    for i = cooldown, 1, -1 do
        statusLabel.Text = "   ⏳ COOLDOWN: " .. i .. "s"
        statusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
        task.wait(1)
    end
    
    isOnCooldown = false
    statusLabel.Text = "   ●  SYSTEM READY"
    statusLabel.TextColor3 = color5
end

-- ============================================================
-- PROXIMITY PROMPT TRIGGER
-- ============================================================
proximityPromptService.PromptTriggered:Connect(function(prompt, player)
    if player ~= localPlayer then return end
    if not v2 then return end
    if isOnCooldown then
        warn("[CAT HUB] Cooldown active")
        return
    end
    
    warn("[CAT HUB] Proximity triggered — TP starting!")
    task.spawn(executeTeleport)
end)

-- ============================================================
-- STARTUP LOG
-- ============================================================
print("================================")
print("     CAT HUB • ANTI GUARD       ")
print("     Made by Chris              ")
print("================================")
print(" ANTI GUARD: ON (default)")
print(" WAYPOINTS: " .. #v1)
print("==================================")
warn("🐱 [CAT HUB] ANTI GUARD ACTIVE")
